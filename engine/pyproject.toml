[project]
name = "haqdaar"
version = "0.1.0"
description = "Haqdaar engine — grounded, refusing, deterministic scheme eligibility."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.12",
    "pyyaml>=6",
]

[project.optional-dependencies]
dev = ["pytest>=8", "httpx>=0.27"]
# The API layer is optional: the deterministic engine imports none of it.
api = ["fastapi>=0.115", "uvicorn>=0.30", "python-multipart>=0.0.9"]
# Local OCR. The tesseract BINARY is a separate system install (see RUNNING.md);
# without it the app reports ocr_available=false and reads nothing, which is honest.
ocr = ["pytesseract>=0.3.10", "pillow>=10"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["haqdaar*"]

[tool.pytest.ini_options]
# pythonpath lets `pytest engine/tests` work from the repo root with no install step.
pythonpath = ["."]
testpaths = ["tests"]
